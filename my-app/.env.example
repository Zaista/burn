# Copy to .env.production.local (gitignored via the *.local pattern) and
# fill in your deployed backend's URL before running `npm run build` for a
# production deploy. Vite bakes this in at build time.
VITE_API_BASE_URL=https://your-app.appspot.com
